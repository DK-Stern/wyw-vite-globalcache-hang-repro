import { createRoot } from 'react-dom/client';
import { StyledAppLayout as AppLayout } from './components/AppLayout';

const root = createRoot(document.getElementById('root'));
root.render(<AppLayout />);
